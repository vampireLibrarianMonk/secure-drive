<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Full-text search for the Emergency Archive: SQLite + FTS5 index maintained entirely inside the encrypted vault (spec section 9), document text extraction (PDF, TXT, Markdown, DOCX, XLSX, PPTX, HTML), and safe query building.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Data.Sqlite" />
    <PackageReference Include="PdfPig" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\EmergencyArchive.Crypto\EmergencyArchive.Crypto.csproj" />
    <ProjectReference Include="..\EmergencyArchive.Integrity\EmergencyArchive.Integrity.csproj" />
  </ItemGroup>

</Project>
